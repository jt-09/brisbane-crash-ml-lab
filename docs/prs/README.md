# Bootstrap PR summaries live here during the autonomous build.

Write one file per planned PR, e.g. `01-bootstrap.md` … `08-reporting-release.md`.

If GitHub PRs are created with `gh`, still keep these summaries as the local audit trail.
